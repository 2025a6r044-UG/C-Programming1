#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a=0,b=1,c,n;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        if(i<=1){
            printf("%d ",i);
        }
        else{
            c=a+b;
            a=b;
            b=c;
            printf("%d ",c);
        }
    }
    return 0;
}

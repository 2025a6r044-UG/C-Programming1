//WAP to find if a number is prime or not using function
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int CheckPrime(int num){
    if(num==1){
        return 0;
    }
    int flag=0;
    for(int i=2;i<=num/2;i++){
        if(num%i==0){
            flag=1;
            break;
        }
    }
    if(flag==0)
        return 1;
    else
        return 0;
}
int main(){
    int srt,end,flag=0;
    scanf("%d %d",&srt,&end);
    for(int i=srt;i<=end;i++){
        if(CheckPrime(i)){
            printf("%d ",i);
            flag=1;
        }
    }
        if(flag==0){
        printf("No prime numbers found");
    }
    return 0;
}

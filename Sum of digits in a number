//WAP to find sum of all digits in a number
#include <stdio.h>
int main() {
    printf("Enter number:");
    int n,i,sum=0;
    scanf("%d",&n);
    while(n!=0){
        i=n%10;
        sum+=i;
        n=n/10;
    }
    printf("%d",sum);
    return 0;
}

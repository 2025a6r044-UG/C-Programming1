#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b,*ptr1,*ptr2;
    ptr1=&a;
    ptr2=&b;
    scanf("%d %d",ptr1,ptr2);
    printf("%d",*ptr1+*ptr2);
    return 0;
}
